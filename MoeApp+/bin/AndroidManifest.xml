<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fm.moe.luhuan"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        >
        
        <activity
            android:name=".activities.AppInit"
            android:label="@string/title_activity_oauth" >
            <intent-filter>
	            <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".activities.MusicBrowse"
            android:label="@string/browse"
            ></activity>
        <service android:name="fm.moe.luhuan.service.MusicService" />
        <service android:name="fm.moe.luhuan.service.MyService" />
        
    </application>

</manifest>